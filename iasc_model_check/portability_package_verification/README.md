# Separate extracted-archive verification

These Git-only records verify the final portability-revision ZIP after extraction on Windows and Linux. They are not inside that ZIP's self-contained manifest and do not change its frozen study or add model/application runs. PACKAGE_INDEX.json and FINAL_PACKAGE_VERIFICATION.json bind the exact ZIP digest. The earlier packaging_reproduction/ directory records the first S10 release's Windows TLC/exact reproduction and remains historical evidence.
